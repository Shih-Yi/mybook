<%= form_for @event,:url => event_path(@event), :method=>:patch do |f| %>

  <%= f.label :name %><br>
  <%= text_field_tag :name %><br>
  <br>
  <%= f.label :body %><br>
  <%= text_area_tag :body %><br>
  <br>
  <%= f.submit %>
<% end %>
